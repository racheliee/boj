#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

long long int n, r;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> n >> r;
    cout << n + 2*r-1;

    return 0;
}